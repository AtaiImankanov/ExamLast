@model List<TransJ>

@if (Model.Count == 0)
{
    <div>пока нету журнала</div>
}else{
@foreach (var c in Model)
{
  <div>| Date @c.dateCreated | @c.WhoSend.UserName | @c.WhoGet.UserName | @c.amountOfTrans | </div>
  
}
}
