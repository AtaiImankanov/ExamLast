
@{
    ViewData["Title"] = "Transaction";
}
<div>

    PersnalBill
 <input id="Bill"  asp-validation-for="PersonalBill" />
 </div>
 <div>
     Amount
 <input id="Amount"  asp-validation-for="PersonalBill" />
 </div>
 <div>
  <input  type="button" onclick="TopUp()"value="Transaction" />
  </div>
  @section Scripts
{   
    <script>
        function TopUp() {
            $.ajax({
                type: 'POST',
                url: '@Url.Action("Transaction", "Home")',            
                data: { 'amount': $('#Amount').val(), 'bill': $('#Bill').val() },
                success: console.log('все ок')
            });
        }
    </script>
    @{

        await Html.RenderPartialAsync("_ValidationScriptsPartial");

    }

}